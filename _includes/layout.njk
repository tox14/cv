<!doctype html>
<html lang="{{ lang }}">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ translations[lang].header.title }}</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <header class="block">
        <div class="block_left">
          <h2 class="uppercase">{{ translations[lang].header.resume }}</h2>
        </div>
        <div class="block_right">
          <h1>{{ translations[lang].header.name }}</h1>
          <h2 class="uppercase">{{ translations[lang].header.position }}</h2>
        </div>
      </header>

      <div class="language-switcher">
        <a class="language-btn{% if lang == 'ru' %} active{% endif %}" href="/ru/">RU</a>
        <a class="language-btn{% if lang == 'en' %} active{% endif %}" href="/en/">EN</a>
      </div>
    </div>
  </body>
</html>
